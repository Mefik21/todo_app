import Logo from './Logo';

const Header = () => {
    return (
        <nav className="d-flex justify-content-center align-items-center px-10 color-brand">
            <Logo />
        </nav>
    );
};

export default Header;
